## 1. Provider 配置与设置存储

- [x] 1.1 扩展现有设置结构，新增 Provider 类型、`baseURL`、认证信息、翻译模型和 TTS 模型字段
- [x] 1.2 在扩展面板中新增 Provider 配置入口，并支持云端、自定义 HTTP API、OpenAI-compatible 与本地 `localhost` 服务
- [x] 1.3 为 Provider 配置增加基础校验与连接错误提示，确保无效配置不能发起配音任务

## 2. 中文配音任务链路

- [x] 2.1 复用现有中文字幕读取能力，整理为可提交的字幕文本与时间轴载荷
- [x] 2.2 实现中文配音任务创建、轮询状态与结果读取的统一客户端
- [x] 2.3 在面板中增加“生成中文配音”任务入口，并展示排队、处理中、成功、失败等状态
- [x] 2.4 定义并接入最小结果契约，至少消费中文配音音频地址、可选字幕地址和分段元数据

## 3. 中文配音播放控制

- [x] 3.1 新增外挂中文音频控制器，支持与 `video` 的播放、暂停、跳转和倍速同步
- [x] 3.2 在启用中文配音时实现原声静音或恢复默认音频行为的切换
- [x] 3.3 确保启用中文配音期间继续显示中文字幕，并在有配音结果字幕时保留至少一种中文字幕显示路径
- [x] 3.4 为同步漂移和结果不可用场景增加兜底处理，保证用户能随时切回原声

## 4. 验证与文档

- [x] 4.1 补充 Provider 配置、任务状态与播放同步的手工验证清单或测试
- [x] 4.2 更新 `README`，说明中文配音能力、Provider 配置方式和首期不包含 ASR 的范围
- [x] 4.3 记录后续扩展点，包括 ASR、说话人分离和更细粒度时间对齐的接口预留
