## 1. 多选交互基础

- [x] 1.1 在 `host.js` 宿主页悬浮时间轴中增加多选状态、锚点状态和选中样式
- [x] 1.2 实现普通点击跳转、`Ctrl/Cmd` 单选切换和 `Shift` 连选逻辑
- [x] 1.3 增加清空选择与分享按钮的可用性状态控制

## 2. 分享图片生成

- [x] 2.1 设计基于 `canvas` 的双语字幕分享图片版式与文本换行逻辑
- [x] 2.2 实现将选中的时间轴条目渲染为 PNG 图片并触发下载
- [x] 2.3 为无选中项、长字幕和多条连续字幕场景补充边界处理

## 3. 体验整合与验证

- [x] 3.1 确保多选分享不破坏现有定位当前、点击跳转、暂停自由滚动和刷新恢复行为
- [x] 3.2 校验折叠、拖动、改尺寸后的悬浮窗仍能正常选择和分享
- [x] 3.3 运行 `node --check host.js`、`node --check content.js` 并整理手动验证清单

## 手动验证清单

- 非全屏状态下，普通点击时间轴条目仍然跳转视频
- `Ctrl/Cmd` 点击可增删单条，`Shift` 点击可连续选择
- 选中后“清空选择”“分享图片”按钮可用，无选中时禁用
- 分享图片可下载 PNG，且包含时间戳、英文、中文与自动换行
- 暂停后可自由滚动，点击“定位当前”仍可回到当前字幕
- 折叠、拖动、改尺寸后，选择与分享功能仍正常
