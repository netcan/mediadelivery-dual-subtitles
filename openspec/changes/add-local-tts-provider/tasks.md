## 1. 本地 Provider 服务脚手架

- [ ] 1.1 在仓库中新增本地 Provider 服务目录、启动入口与基础配置
- [ ] 1.2 实现 `POST /jobs` 与 `GET /jobs/:id` 的基础路由，并对齐扩展当前任务契约
- [ ] 1.3 增加本地服务的错误处理与输入校验，确保无效字幕和模型不可用场景可返回明确错误

## 2. 本地 TTS 模型适配层

- [ ] 2.1 定义统一的本地 TTS 适配器接口，隔离模型调用差异
- [ ] 2.2 实现首期推荐的 CosyVoice 风格适配器接入路径
- [ ] 2.3 在模型调用前加入字幕清洗、分句或基础口语化预处理步骤
- [ ] 2.4 为模型调用失败、超时和预处理失败增加标准化任务失败返回

## 3. 结果输出与扩展联调

- [ ] 3.1 实现本地输出目录与静态文件暴露，生成可供扩展访问的 `audioUrl`
- [ ] 3.2 支持返回可选 `subtitleUrl`、`segments` 与 `audioOffsetSec` 等最小元数据
- [ ] 3.3 用当前扩展的 `localhost` Provider 配置完成一次端到端联调
- [ ] 3.4 验证同步与异步两种任务返回路径至少有一种稳定可用

## 4. 文档与后续扩展说明

- [ ] 4.1 更新文档，说明本地 Provider 服务的启动方式、模型准备和扩展配置步骤
- [ ] 4.2 补充手工验证清单，覆盖任务创建、任务查询、音频播放和失败处理
- [ ] 4.3 记录后续可扩展项，包括更多本地 TTS 模型、ASR 增强和高级混音能力
